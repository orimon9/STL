#include<bits/stdc++.h>

using namespace std;

int main()

{
    int N;
    cin>>N; //read numr of string

    vector<string>strings(N); //create a vector to store the strings
    for(int i=0;i<N;i++){
        cin>>strings[i];
    }

    for(int i=N-1; i>=0; i--){
        cout<<strings[i]<<endl;
    }

    return 0;

}
